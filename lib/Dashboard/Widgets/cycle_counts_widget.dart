import 'package:flutter/material.dart';

class CycleCountsWidget extends StatelessWidget {
  const CycleCountsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Image.asset('assets/images/CycleCountMenuIcon.png'),
    );
  }
}
